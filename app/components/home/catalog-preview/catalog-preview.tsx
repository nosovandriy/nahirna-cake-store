import CatalogItem from "@catalog/catalog-item/catalog-item";

const CatalogPreview = () => {
  const isFullCatalog = false;
  return <CatalogItem isFullCatalog={isFullCatalog} />;
};

export default CatalogPreview;
