export interface Catalog {
  id: string;
  popularity: string;
  title: string;
  price: number;
  weights: number[];
  description: string;
  ingredients: string;
  imageTitle: string;
  images: string[];
}
