export interface Catalog {
  id: string;
  popularity: number;
  title: string;
  price: number;
  weights: number[];
  description: string;
  ingredients: string;
  imageTitle: string;
  imageBlur: string;
  images: string[];
}
