export interface Catalog {
  id: string;
  popularity: number;
  title: string;
  price: number;
  weights: number[];
  description: string;
  ingredients: string;
  imageTitle: string;
  images: string[];
}
